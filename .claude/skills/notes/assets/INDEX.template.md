# 知識筆記索引（_notes/）

開發討論過程延伸學到、與專案本身較無關的知識，先扁平收在這裡。

> 此資料夾以 `_` 開頭，`.gitignore` 的 `_*` 規則會自動排除，**不進 git /
> build context**——是個人本地知識，不污染專案。日後可整夾或單篇搬進你
> 的個人知識庫（連結用裸檔名、筆記自足，搬移不斷鏈）。
>
> 機制與規格見 [`.claude/skills/notes/SKILL.md`](../.claude/skills/notes/SKILL.md)（單一真相來源）。
> 本檔是人工索引，由 notes skill 在每次增/改/刪筆記時同步，亦是 agent
> 回找知識的入口（先讀本檔定位、再開該篇）。

## （尚無筆記）

_最後更新：—_
